module.exports = {
    // Use react-scripts' Jest transformer by default
    transform: {
      "^.+\\.(js|jsx|ts|tsx)$": "<rootDir>/node_modules/babel-jest"
    },
    // Don't transform some node_modules, but allow specific React-related packages
    transformIgnorePatterns: [
      "/node_modules/(?!(@react-oauth|react-router|react-calendar|react-dom)/)"
    ],
    // Setup file for testing environment
    setupFilesAfterEnv: [
      "<rootDir>/src/setupTests.js"
    ],
    // Tell Jest to handle these file extensions
    moduleFileExtensions: [
      "js",
      "jsx"
    ],
    // Test environment for React
    testEnvironment: "jsdom"
  };